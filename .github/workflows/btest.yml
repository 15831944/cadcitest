name: BRL-CAD

on: [push]

jobs:
  build-mac:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v1
    - uses: lukka/get-cmake@latest
    - name: configure
      run: mkdir build && cd build && cmake --version && cmake .. && more CMakeCache.txt && ls -R
    - name: build
      run: cd build && make distcheck
      run: more distcheck-enableall_debug.log && more distcheck-enableall_release.log
      #- name: configure2
      #run: cd build && make distclean && cmake .. -DENABLE_ALL=ON && more CMakeCache.txt && ls -R
      #- name: build2
      #run: cd build && make && make check
